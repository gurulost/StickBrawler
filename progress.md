Original prompt: Do it!

- 2026-03-19: Started Phase 1 of the character visual upgrade roadmap.
- 2026-03-19: Fixed customizer accessory preview hook-order crash by moving accessory material hooks into a stable child component in `client/src/game/stickfigure/Head.tsx`.
- 2026-03-19: Reduced preview topology swap instability by keying preview-only `StickFigure`, `ParticleEffect`, and `AuraEffect` remounts in `client/src/components/preview/CharacterPreview.tsx`.
- 2026-03-19: `npm run check` passes after the Phase 1 stability patches.
- 2026-03-19: Browser repro passed for `none -> Wizard Hat`, `Wizard Hat -> Demon Horns`, Robot/Ethereal/Crystal body-style swaps, and Player/CPU preview tab changes without the old React hook-order crash or Three.js buffer-resize spam.
- 2026-03-19: Browser error output after the repro loop is still limited to the pre-existing unsigned-in `401 /api/auth/me` and `404 /api/economy/:profileId` requests.
- 2026-03-19: A `THREE.WebGLRenderer: Context Lost.` log appears during preview remounts, but the preview recovers and remains visible; treat this as a follow-up cleanup item rather than a blocker.
- 2026-03-19: Validation complete: `npm run check` and `npm test` both pass, with `8/8` tests green.
- 2026-03-19: Phase 2 shipped a reusable `InkPart` primitive plus a dedicated `AccessoryRig` so accessories are mounted to head/back/hand anchors instead of all being rendered at the head.
- 2026-03-19: Upgraded the character look with style-specific head and torso geometry for angular, streamlined, rounded, mechanical, translucent, and crystalline body styles, and wired `shoulderWidth` into actual limb spacing.
- 2026-03-19: Upgraded the preview scene with a larger framed canvas, pedestal stage, halo backdrop, stronger lighting, and customizer preview-card sizing so fighters read more like featured loadouts than debug renders.
- 2026-03-19: Browser smoke after the Phase 2 pass shows the new preview card rendering cleanly with the upgraded crystal silhouette and correctly mounted Flame Sword.
- 2026-03-19: Added reactive accessory posing in `AccessoryRig` so sword, shield, cape, wings, halo, visor, horns, crown, and hat respond to attack/jump/block/speed inputs instead of staying rigid.
- 2026-03-19: Enabled showcase animation inside the customizer preview so loadouts cycle through motion and the new accessory posing is visible without entering a match.
- 2026-03-19: Browser smoke after the accessory-motion pass shows the preview cycling through different sword poses over time while console errors remain limited to the pre-existing unsigned-in `401 /api/auth/me` and `404 /api/economy/:profileId` requests.
- 2026-03-19: Validation rerun after the motion/showcase pass is still clean: `npm run check` and `npm test` both pass, with `8/8` tests green.
- 2026-03-19: Kept both customizer preview canvases mounted with `forceMount` and only animate the active tab, which stops additional `THREE.WebGLRenderer: Context Lost.` logs when switching Player/CPU preview tabs.
- 2026-03-19: Browser smoke confirms tab switching no longer increases console noise; the remaining `Context Lost` logs occur on route transition into the customizer because the landing-page previews are torn down.
- 2026-03-19: Fixed the first-open customizer teardown in `client/src/game/Menu.tsx` by mounting the customization panel synchronously in the same click handler instead of waiting for an effect-driven second render.
- 2026-03-19: Browser smoke after the `Menu.tsx` fix confirms `Open Ink Customizer` no longer adds `THREE.WebGLRenderer: Context Lost.` logs; the console now stays limited to the pre-existing unsigned-in `401 /api/auth/me` noise during the hero-to-customizer transition.
- 2026-03-19: Verified steady-state preview persistence after the fix: opening the customizer leaves 4 canvases in the DOM (2 hidden hero previews, 2 visible customizer previews), so route transitions and tab switches no longer tear down preview renderers.
- 2026-03-19: Match-presentation pass landed in `client/src/game/stickfigure/AccessoryRig.tsx` and `client/src/game/StickFigure.tsx`, adding stronger sword trails, shield orbit effects, cape/wings deformation, and taunt-only halo/crown/horn flourishes, plus accessory-specific brush accents for attack, guard, hit, and taunt events.
- 2026-03-19: Validation after the match-presentation pass is clean: `npm run check` passes and `npm test` passes (`8/8`).
- 2026-03-19: Browser smoke covered owned-accessory paths: customizer preview with `Demon Horns` active, then `Flame Sword` equipped into a live solo match with movement/attack/special/guard input burst.
- 2026-03-19: Follow-up found during the live match smoke: leaving the customizer/menu flow for the separate lobby/arena route still emits `THREE.WebGLRenderer: Context Lost.` logs because the menu preview canvases are intentionally torn down when the game scene replaces the menu tree.
- 2026-03-19: Match-readability pass landed in `client/src/game/GameManager.tsx`, `client/src/App.tsx`, and `client/src/game/UI.tsx`: the fight camera now dynamically tracks fighter midpoint/spread instead of staying on a fixed wide shot, the gameplay camera starts from a tighter angle, and the HUD gained live `Special Ready`, `Guard Critical`, and `Guard Broken` callouts plus a properly sized score strip.
- 2026-03-19: Browser smoke after the readability pass confirmed the fighters render materially larger in live matches and the new HUD callouts appear correctly during guard-break / full-special states; validation rerun stayed clean with `npm run check` and `npm test` both passing (`8/8`).
- 2026-03-19: Remaining visual follow-up from the browser pass is no longer pure framing; platform columns can still occlude fighters in some angles now that the camera is closer, so the next match-presentation win is likely stage-aware camera bias, occlusion handling, or lighter central-platform geometry.
- 2026-03-19: Added stage-aware occlusion handling for open arenas in `client/src/game/Arena.tsx`; multi-level platforms and support posts now fade toward ghosted translucent materials when they sit between the gameplay camera and either fighter, which keeps the closer camera framing without reintroducing hidden-character shots.
- 2026-03-19: Browser smoke after the occlusion pass confirmed the tall center platform now ghosts out while fighters move beneath it, with no new console errors beyond the existing unsigned-in `401 /api/auth/me` and `404 /api/economy/:profileId` noise.
- 2026-03-19: Folded the floating `Show Controls`, `Debug`, and `Low Graphics` buttons into the main top-left HUD cluster in `client/src/game/UI.tsx`, which keeps the fight space cleaner during screenshots and live play; validation rerun stayed clean with `npm run check` and `npm test` both passing (`8/8`).
- 2026-03-19: The standalone `$WEB_GAME_CLIENT` flow is still blocked locally because the repo environment does not have the `playwright` package installed; browser verification continues through the built-in Playwright tools instead.
- 2026-03-20: Added explicit authored hitbox sockets across the move table, exported shared combat spatial sampling from `client/src/combat/hitResolver.ts`, and wired a live `CombatDebugOverlay` into matches so socket/hurtbox/hitbox tuning is visible in-engine instead of inferred from logs.
- 2026-03-20: Landed the requested open-arena readability cleanup in `client/src/game/Arena.tsx`: clearer combat-lane strip, sparser edge decoration, lighter/thinner supports, reduced central clutter, and brighter underside/platform separation for the closer 2.5D camera.
- 2026-03-20: Fixed the debug telemetry seam end-to-end by teaching `server/telemetry.ts` and `server/routes.ts` to accept mixed hit/input/tech events, adding route coverage in `tests/server/telemetry-routes.test.ts`, batching client sends in `client/src/game/GameManager.tsx`, and making runtime input telemetry change-driven plus rate-limited in `client/src/game/matchRuntime.ts`.
- 2026-03-20: Validation after the polish/readability pass is clean: `npm run check` passes, `npm test -- --runInBand` passes with `17/17` tests green, and Playwright smoke on the live app confirms debug mode no longer produces `422 /api/telemetry` spam while the arena/debug overlay remain readable.
- 2026-03-20: Finished the remaining content pass in `client/src/game/stickfigure/movePresentation.ts` by adding bespoke pose clips for the rest of the current authored hero/villain move IDs and exporting clip-resolution helpers so the move-presentation roster is testable instead of implicit.
- 2026-03-20: Confirmed presentation coverage across the whole current move table: a `npx tsx -e ...` audit shows all 28 authored moves in `client/src/combat/moves.ts` resolve to a presentation clip after the alias/clip expansion.
- 2026-03-20: Added per-theme stage readability tuning in `client/src/game/arenas/index.ts` and `client/src/game/Arena.tsx`; Sunset Bloom, Aurora Flux, and Containment now each carry different lane/backdrop/support/glow treatments while preserving the tighter 2.5D combat framing.
- 2026-03-20: Added `tests/client/presentation-polish.test.ts` to lock in the new move-clip coverage plus stage-tuning contract, and reran validation cleanly: `npm run check` plus `npm test -- --runInBand` now pass with `21/21` tests green.
- 2026-03-20: Playwright smoke covered live matches on Sunset Bloom, Aurora Flux, and Containment Arena with debug HUD enabled; screenshots show distinct stage composition per arena, an injected `j` input still surfaced `hero_jab_1` through live move state, and no new browser failures appeared beyond the known guest-mode `401 /api/auth/me`, `404 /api/economy/:profileId`, and scene-swap `THREE.WebGLRenderer: Context Lost.` noise.
- TODO: Reduce remaining unsigned-in customizer/menu console noise from `401 /api/auth/me` and `404 /api/economy/:profileId` requests so visual smoke runs stay signal-rich.
- TODO: Decide whether the menu-to-lobby `Context Lost` logs matter enough to fix; unlike the hero/customizer bug, this is full-scene teardown into gameplay rather than a same-screen tab/panel transition.
- TODO: If new authored moves or arena themes are added, extend `client/src/game/stickfigure/movePresentation.ts` and `tests/client/presentation-polish.test.ts` in the same change so full clip/tuning coverage stays enforced.
